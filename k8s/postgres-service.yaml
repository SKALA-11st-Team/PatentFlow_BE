apiVersion: v1
kind: Service
metadata:
  name: team11-patentflow-postgres-svc
  namespace: patentflow
spec:
  type: ClusterIP
  selector:
    app: team11-patentflow-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
