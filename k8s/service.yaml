apiVersion: v1
kind: Service
metadata:
  name: 11-team-patentflow-be-svc
  namespace: patentflow
spec:
  type: ClusterIP
  selector:
    app: 11-team-patentflow-be
  ports:
    - name: http
      port: 80
      targetPort: http
