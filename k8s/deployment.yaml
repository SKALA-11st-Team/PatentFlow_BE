apiVersion: apps/v1
kind: Deployment
metadata:
  name: patentflow-be
  namespace: patentflow
  labels:
    app: patentflow-be
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: patentflow-be
  template:
    metadata:
      labels:
        app: patentflow-be
    spec:
      imagePullSecrets:
        - name: harbor-regcred
      containers:
        - name: patentflow-be
          image: harbor.example.com/patentflow/patentflow-be:placeholder
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: patentflow-be-config
            - secretRef:
                name: patentflow-be-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
